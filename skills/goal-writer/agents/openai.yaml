interface:
  display_name: "Goal Writer"
  short_description: "Writes verifiable, bounded /goal prompts."
  default_prompt: "Use $goal-writer to turn this task into a strong Codex /goal prompt."

policy:
  allow_implicit_invocation: true
